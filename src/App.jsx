import Card from './components/Card';

export default function App() {
  return (
    <section>
      <Card />
    </section>
  );
}
// content -> content may change so i better isolate it
// footer -> maybe instead of links I want something else
